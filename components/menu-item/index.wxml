<view class='menu-item' bindtap='select' data-id='{{data.id}}' data-name='{{data.name}}'>
  <view class='menu-item-content'>
    <image src="{{data.image}}"></image>
    <view class='menu-txt'>{{data.name}}</view>
  </view>
</view>
